from distutils.core import setup
# from setuptools import setup, find_packages

setup(name='Closet',
      version='0.0.1',
      py_modules=['Closet'],
      install_requires=[
            'numpy',
      ],
      scripts=[
      ],
)
