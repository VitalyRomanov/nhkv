from distutils.core import setup
# from setuptools import setup, find_packages

setup(
      name='nhkv',
      version='0.0.1',
      py_modules=['nhkv'],
      install_requires=["dill"],
      scripts=[],
)
